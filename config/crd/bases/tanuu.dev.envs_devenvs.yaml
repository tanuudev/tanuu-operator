---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: devenvs.tanuu.dev.envs
spec:
  group: tanuu.dev.envs
  names:
    kind: Devenv
    listKind: DevenvList
    plural: devenvs
    singular: devenv
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Devenv is the Schema for the devenvs API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: DevenvSpec defines the desired state of Devenv
            properties:
              cloudProvider:
                type: string
              gpu:
                type: boolean
              name:
                description: Foo is an example field of Devenv. Edit devenv_types.go
                  to remove/update
                type: string
              size:
                type: string
            type: object
          status:
            description: DevenvStatus defines the observed state of Devenv
            properties:
              cloudProvider:
                type: string
              controlPlane:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                items:
                  type: string
                type: array
              gpus:
                items:
                  type: string
                type: array
              ipAddress:
                type: string
              kubeconfig:
                type: string
              services:
                items:
                  type: string
                type: array
              status:
                type: string
              workers:
                items:
                  type: string
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
